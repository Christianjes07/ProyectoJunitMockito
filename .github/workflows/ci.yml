name: Prueba CI
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Verificar conexión
        run: echo "Workflow funcionando correctamente"
